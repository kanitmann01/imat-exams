/* Platform configuration. The password gates the app (a deterrent, not security).
   Supabase values enable optional cloud sync; empty means local-only mode. */
window.IMAT_CONFIG = {
  password: "imat2026",
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
